<div class="main-lists-edit">
  <div class="lists-edit-container">
    <h2 class="form-heading">編集する</h2>
    <%= form_tag("/lists/#{@list.id}/update") do %>
      <div class="form">
        <div class="form-bady">
          <textarea name="content"><%= @list.content %></textarea>
          <input type="submit" value="編集">
        </div>
      </div>
    <% end %>
  </div>
</div>
